
<?php

include("conn.php");

// Create table
$sql="CREATE TABLE regtable(name CHAR(30),pass CHAR(30),email CHAR(30))";

// Execute query
if (mysqli_query($con,$sql))
  {
  echo "Table regtable created successfully";
  }
else
  {
  echo "Error creating table: " . mysqli_error($con);
  }
?>
